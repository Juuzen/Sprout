package utils;

import com.hcifedii.sprout.enumerations.TaskStatus;

import io.realm.RealmObject;

/*
public class TaskStatusEnum extends RealmObject {
    private String taskStatus = TaskStatus.UNCOMPLETED.name();

    public void saveStatus(TaskStatus val) {
        this.taskStatus = val.toString();
    }

    public TaskStatus getEnum() {
        return TaskStatus.valueOf(taskStatus);
    }
}
*/